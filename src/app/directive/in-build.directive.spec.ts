import { InBuildDirective } from './in-build.directive';

describe('InBuildDirective', () => {
  it('should create an instance', () => {
    const directive = new InBuildDirective();
    expect(directive).toBeTruthy();
  });
});
